import { colors } from "./src/colors.ts";
import { readdir } from "node:fs/promises";

console.log(colors.bold('(yrg)'), colors.red('yara-rule-generator'), colors.gray('(v0.0.0-dev)'));

(async () => {
})();